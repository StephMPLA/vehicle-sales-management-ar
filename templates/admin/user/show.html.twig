{% extends 'base.html.twig' %}

{% block title %}Fiche client{% endblock %}

{% block body %}
    <div class="min-h-screen bg-gray-100 p-6">

        <div class="max-w-6xl mx-auto space-y-6">

            {# ===== HEADER ===== #}
            <div class="bg-white rounded-xl shadow p-6 flex justify-between items-center">

                <div>
                    <h1 class="text-2xl font-bold text-gray-800">
                        👤 {{ client.firstname }} {{ client.lastname }}
                    </h1>
                </div>

                <a href="{{ path('app_admin_dashboard') }}"
                   class="text-sm bg-gray-200 hover:bg-gray-300 px-4 py-2 rounded-lg">
                    ← Retour dashboard
                </a>
            </div>


            {# ===== GRID ===== #}
            <div class="grid md:grid-cols-3 gap-6">

                {# ================= INFOS PRINCIPALES ================= #}
                <div class="md:col-span-2 bg-white rounded-xl shadow p-6 space-y-4">

                    <h2 class="font-semibold text-lg border-b pb-2">
                        Informations client
                    </h2>

                    <div class="grid md:grid-cols-2 gap-4 text-sm">

                        <div>
                            <p class="text-gray-500">Nom</p>
                            <p class="font-medium">{{ client.lastname ?? '—' }}</p>
                        </div>

                        <div>
                            <p class="text-gray-500">Prénom</p>
                            <p class="font-medium">{{ client.firstname ?? '—' }}</p>
                        </div>

                        <div>
                            <p class="text-gray-500">Email</p>
                            <p class="font-medium">
                                <a href="mailto:{{ client.email }}"
                                   class="text-blue-600 hover:underline">
                                    {{ client.email }}
                                </a>
                            </p>
                        </div>

                        <div>
                            <p class="text-gray-500">Téléphone</p>
                            <p class="font-medium">
                                <a href="tel:{{ client.phone }}"
                                   class="text-blue-600 hover:underline">
                                    {{ client.phone ?? '—' }}
                                </a>
                            </p>
                        </div>

                    </div>
                </div>


                {# ================= ACTIONS RAPIDES ================= #}
                <div class="bg-white rounded-xl shadow p-6 space-y-4">

                    <h2 class="font-semibold text-lg border-b pb-2">
                        Actions rapides
                    </h2>

                    <div class="flex flex-col gap-3">

                        <a href="mailto:{{ client.email }}"
                           class="bg-blue-600 hover:bg-blue-700 text-white text-center py-2 rounded-lg">
                            ✉ Envoyer un email
                        </a>

                        <a href="tel:{{ client.phone }}"
                           class="bg-green-600 hover:bg-green-700 text-white text-center py-2 rounded-lg">
                            📞 Appeler
                        </a>

{#                        <button#}
{#                            onclick="window.print()"#}
{#                            class="bg-gray-700 hover:bg-gray-800 text-white py-2 rounded-lg">#}
{#                            🖨 Imprimer fiche client#}
{#                        </button>#}

                        <a href="{{ path('admin_client_pdf',{id: client.id}) }}"
                           class="bg-green-600 hover:bg-green-700 text-white text-center py-2 rounded-lg">
                            📄 Générer le PDF client
                        </a>


                    </div>
                </div>

            </div>


            {# ================= ADRESSE ================= #}
            <div class="bg-white rounded-xl shadow p-6">

                <h2 class="font-semibold text-lg border-b pb-2 mb-4">
                    Adresse
                </h2>

                <p class="text-sm">
                    {{ client.address ?? '—' }}<br>
                    {{ client.postalCode ?? '' }}
                    {{ client.city ?? '' }}
                </p>

            </div>


            {# ================= INFOS COMPTE ================= #}
            <div class="bg-white rounded-xl shadow p-6">

                <h2 class="font-semibold text-lg border-b pb-2 mb-4">
                    Informations du compte
                </h2>

                <div class="grid md:grid-cols-3 gap-4 text-sm">

                    <div>
                        <p class="text-gray-500">Compte créé le</p>
                        <p class="font-medium">
                            {{ client.createdAt|date('d/m/Y') }}
                        </p>
                    </div>

                    <div>
                        <p class="text-gray-500">Dernière mise à jour</p>
                        <p class="font-medium">
                            {{ client.createdAt ? client.createdAt|date('d/m/Y') : '—' }}
                        </p>
                    </div>



                </div>

            </div>

        </div>
    </div>
{% endblock %}
